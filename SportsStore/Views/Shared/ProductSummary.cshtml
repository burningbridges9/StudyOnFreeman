@model Product
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>
            @Model.Name
        </h4>
    </div>
    <div class="card-text p-1">@Model.Price.ToString("c") - @Model.Description</div>
</div>